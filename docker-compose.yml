services:
    prerender:
        image: ngamesfr1/prerender-alpine
        ports:
        - 3000:3000
        environment:
        - MEMORY_CACHE=0
        - BLOCK_RESOURCES=0
        - CACHE_MAXSIZE=1000
        - CACHE_TTL=6000
        restart: unless-stopped
